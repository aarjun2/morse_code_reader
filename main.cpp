#include "BST.h"

int main(void) {
	//  calling BST using two types with a template, through a constructor which is called on 
	//after an object is instantiated
	BST<char,string> mytree;
	return 0;
}